순차파일(이론적 방법)

실제로는 인덱스를 만들어서 관리를 한다.
33:00
인덱스는 크게 2가지로 나눈다.
단일 단계 인덱스(가장 간단)
다단계 인덱스

인덱스에 들어가는 키값과 그 키를 갖은 레코드의 주소(포인터)로 이루어짐.
(데이터 부담이 작다 100만개가 넘어도 <-- 키값 주소값 합쳐야 8Byte)

create index를 이용하여 attribute별로 만들 수 있다.
(사진)을 해석하면
그냥 들어오는대로 데이터는 쌓는다.
인덱스(많이 사용할 것 같은 키값으로 생성)는 소팅해서 관리한다.(소팅되어 있기 때문에 Binary search를 하면 겁나 빠르다.)
인덱스는 꼭 Unique하지 않아도 된다. 다만 다른 레코드만 가리키면 된다.
도메인 카디널리티가 적은 것(ex. 직급)으로 인덱스를 만들면 다만 좋지 못하다.

primary index (기본키를 가지고 만든 인덱스를 특히 지칭)